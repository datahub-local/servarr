{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits"
  ],
  "schedule": [
    "* * * * 6"
  ],
  "automerge": false,
  "prCreation": "immediate",
  "automergeStrategy": "squash",
  "forkProcessing": "enabled",
  "includePaths": [
    "servarr/**"
  ],
  "enabledManagers": [
    "helmv3",
    "helm-values",
    "dockerfile"
  ],
  "packageRules": [
    {
      "matchManagers": [
        "helmv3",
        "helm-values",
        "dockerfile"
      ],
      "matchDatasources": [
        "helm",
        "docker"
      ],
      "matchUpdateTypes": [
        "major",
        "minor",
        "patch"
      ],
      "groupName": "all Helm/Docker dependencies",
      "groupSlug": "all",
      "bumpVersions": [
        {
          "filePatterns": [
            "servarr/Chart.{yaml,yml}",
            "servarr/charts/**/Chart.{yaml,yml}"
          ],
          "matchStrings": [
            "version:\\s(?<version>[^\\s]+)"
          ],
          "bumpType": "{{#if isPatch}}patch{{else}}minor{{/if}}"
        },
        {
          "filePatterns": [
            "servarr/Chart.{yaml,yml}",
            "servarr/charts/**/Chart.{yaml,yml}"
          ],
          "matchStrings": [
            "appVersion:\\s(?<version>[^\\s]+)"
          ],
          "bumpType": "{{#if isPatch}}patch{{else}}minor{{/if}}"
        }
      ]
    }
  ]
}